# 이 프로그램이 쓰는 라이브러리.
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
python-dotenv>=1.0
Pillow>=10.4
# 초상권 안전장치(얼굴 감지)에 쓴다. 없으면 사진이 전부 제외된다.
# 5.x 는 안 쓴다 — 얼굴 감지(YuNet)가 5.0 에서 안 되는 사례가 보고됐다.
opencv-python>=4.9,<5
# playwright 는 넣지 않는다. 브라우저 자동화 도구(insta_login.py)에서만 쓰는데
# 공개 배포판에는 그 도구가 없다. 48MB 를 받게 하고 설치 실패 확률만 올린다.
# 그 도구를 쓸 사람만 따로: pip install playwright
